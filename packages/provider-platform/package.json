{
  "name": "@persistent-codex/provider-platform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "cd ../.. && vitest run packages/provider-platform/src/index.test.ts"
  },
  "dependencies": {
    "@persistent-codex/domain-events": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
